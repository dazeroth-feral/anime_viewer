import initial_state from "../state_initiators/footer_initial_state";

const footer_reducer = (store = initial_state, action) => {
    switch(action.type){
        default:
            return store;
    }
};

export default footer_reducer;